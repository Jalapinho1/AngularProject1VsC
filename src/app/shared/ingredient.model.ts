export class Ingredient {
    // public name: string;
    // public amount: number;
    //
    // constructor(name: string, amount: number) {
    //     this.name = name;
    //     this.amount = amount;
    // }

    //SHROTCUT - automaticly gives us properties with speciefied names (name,amount) and asign values
    constructor(public name: string, public amount: number) {}
}